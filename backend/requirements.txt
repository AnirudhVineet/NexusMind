fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.12

sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
pgvector==0.3.4

celery[redis]==5.4.0
redis==5.0.8

pdfplumber==0.11.4
pymupdf==1.27.2.3
ftfy==6.2.3
langdetect==1.0.9
chardet==5.2.0
langchain-text-splitters==0.3.0

openai==1.51.0
google-generativeai==0.8.3
tenacity==9.0.0

# Phase 2: NER + entity dedup + topic modeling + readability
spacy==3.7.5
https://github.com/explosion/spacy-models/releases/download/en_core_web_trf-3.7.3/en_core_web_trf-3.7.3-py3-none-any.whl
gliner==0.2.13
sentence-transformers==3.1.1
rapidfuzz==3.10.0
bertopic==0.16.4
textstat==0.7.4

passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.9.0

structlog==24.4.0
sentry-sdk[fastapi,celery]==2.14.0
prometheus-client==0.21.0

httpx==0.27.2

# Phase 3.3 – Multi-source ingestion
beautifulsoup4==4.14.3
lxml==6.1.0
youtube-transcript-api==1.2.4
notion-client==3.1.0

# Phase 4 dependencies
reportlab==4.2.5          # PDF generation (Track B) — BSD
networkx==3.4.2           # Graph serialization (Track E) — BSD
pandas==2.2.3             # Edge/node CSV export (Track E) — BSD
feedparser==6.0.11        # RSS ingestion (Track F) — BSD
cryptography==43.0.3      # Fernet encryption for SMTP passwords (Track F) — Apache-2.0
py-vapid==1.9.1           # VAPID key generation for Web Push (Track G) — Apache-2.0
pywebpush==2.0.0          # Web Push notification delivery (Track G) — Mozilla Public License 2.0
Pillow==10.4.0            # Meme image rendering (Track H) — HPND (permissive)
python-docx==1.1.2        # DOCX export for content engine (Track H) — MIT
dateparser==1.2.0         # Date extraction from document text (Track C) — BSD
Jinja2==3.1.4             # HTML templates for research PDF (Track B) — BSD

# Phase 5 dependencies
imageio-ffmpeg==0.5.1     # vendored FFmpeg binary detector (Track A) — BSD-2
moviepy==2.0.0.dev2       # NOTE: only used at smoke-test scope; reel renderer talks to FFmpeg directly. MIT.
pyttsx3==2.97             # Offline Windows SAPI TTS fallback (Track B) — MPL-2.0
# Optional Piper TTS — pip install piper-tts where wheels exist; binary fallback at DATA_DIR/bin/piper.exe
# piper-tts==1.2.0        # uncomment if your platform has wheels

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
asgi-lifespan==2.1.0
