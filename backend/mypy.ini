[mypy]
# Point mypy at the project's virtual environment so it resolves installed
# packages (youtube_transcript_api, fastapi, celery, ...) from .venv rather
# than the global Python interpreter.
python_executable = C:/Users/aniru/OneDrive/Desktop/Projects/NexusMind/backend/.venv/Scripts/python.exe
files = app
namespace_packages = True
explicit_package_bases = True
ignore_missing_imports = True
